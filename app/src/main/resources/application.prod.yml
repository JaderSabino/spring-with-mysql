spring:
  datasource:
    url: jdbc:mysql://mysqldb:3306/spring-with-mysql?useTimezone=true&serverTimezone=UTC&enabledTLSProtocols=TLSv1.2
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect